# 别拿对话记录当记忆：AI 编程的上下文管理教程

## 为什么"对话历史"是个坏主意

很多人用 Claude Code / Cursor 这类工具的第一反应是：把项目相关的一切都堆进同一个对话，舍不得开新会话，怕"AI 忘了之前说的话"。

这个直觉是错的，原因有三个：

1. **对话窗口会关**。不管是手动关掉还是达到长度上限，模型迟早要"重新打开仓库"，那一刻它靠什么恢复上下文，才是决定长项目能不能走远的分水岭。
2. **对话历史是易失的、非结构化的**。关键决策、踩过的坑、约定俗成的规范，混在几十轮"改这个""不对再改那个"的闲聊里，下次想复用根本翻不出来。
3. **Token 成本会失控**。每多一轮对话，模型都要重新"读"一遍前面所有内容。同一批文件被反复读、同一个问题被反复解释，都是纯浪费的 token。

正确的做法是反过来：**对话只是"当前任务"的临时工作区，跑完就扔；真正重要的东西要沉淀成文件，进仓库，下次让模型主动去读，而不是让模型"记得"。**

一句话记住这个原则：

> 新对话打开时，模型该读的是文档，不是聊天记录。

---

## 核心思路：把"记忆"拆成四层，分别落盘

| 层级 | 存什么 | 存在哪 | 谁来读 |
|---|---|---|---|
| 项目常识层 | 技术栈、规范、工作流约定 | `CLAUDE.md` / `AGENTS.md` | 每次开新会话自动读 |
| 架构决策层 | 为什么这么设计、放弃了哪些方案 | `docs/adr/*.md`（ADR） | 涉及相关模块时按需读 |
| 可复用操作层 | 常做的重复性任务怎么做 | Skill（`SKILL.md`） | 触发对应任务时自动调用 |
| 任务交接层 | 当前这个任务做到哪了 | `docs/sessions/handover-*.md` | 开新会话时手动指定去读 |

下面逐层讲怎么写、什么时候写。

---

## 第一层：CLAUDE.md —— 项目的"新员工入职手册"

这是最基础也是最该先做的一件事。作用类似给一个新入职的工程师一份文档，让他不用问人就能上手。

**内容要精简，不要把它写成大百科全书**，否则每次都要多花 token 读一遍没用的东西。建议只放：

```markdown
# 项目说明

## 技术栈
- Node.js 20, Express 4.x, PostgreSQL 16
- 测试：Jest + React Testing Library
- 认证：JWT + bcrypt

## 工作流约定
- 每个任务开新分支，绝不直接提交到 main
- 每次代码变更后必须跑 `npm test`
- Commit 用 Conventional Commits 格式（feat:/fix:/refactor:/docs:）
- 完成后用 `gh pr create` 开 PR，不要自己合并

## 代码规范
- 禁止用 any，TypeScript 严格模式
- 错误处理统一走 `lib/errors.ts` 里的自定义异常类

## 已知坑
- `services/payment` 模块的 webhook 签名校验很脆弱，改动前先看 `docs/adr/0004-payment-webhook.md`
```

**写作原则**：
- 只写"模型每次都需要知道"的东西，不要写"这个模块曾经这样实现过"这种一次性信息（那种放 ADR）
- 定期回顾精简，发现哪条规矩几个月都没用上，就删掉
- 如果项目分多个子服务，可以每个子目录放一个局部的 CLAUDE.md，全局的只放通用规范

---

## 第二层：ADR（架构决策记录）—— 留住"为什么"

CLAUDE.md 回答"现在规矩是什么"，ADR 回答"当初为什么这么定"。

没有这一层，你会发现半年后模型（和人）都会去"优化"一个当初特意绕开的坑，因为没人记得当初为什么要绕开。

**模板**（一个决策一个文件，编号递增，不要事后修改，作废了就写新的一条并互相引用）：

```markdown
# ADR-0004：Payment Webhook 用同步校验而非队列

## 状态
已采纳

## 背景
第三方支付平台的 webhook 到达时需要验证签名。最初设计用消息队列异步处理，
但支付平台要求 5 秒内返回 200，队列引入的延迟会导致重试风暴。

## 决策
Webhook 收到后同步校验签名、同步写库，不经过队列。

## 后果
- 好处：避免支付平台重试风暴
- 代价：写库失败时需要自己实现幂等重试逻辑（见 `services/payment/retry.ts`）
- 如果未来要做多机房部署，这个决策需要重新评估
```

**什么时候该写一条 ADR**：只要一个决策"如果不记录，几个月后大概率会被人（或AI）重新做一遍且做错"，就该写。不是所有决策都值得写，滥写和不写一样没用。

---

## 第三层：Skill —— 把重复劳动打包成"技能"

如果某个操作你一个月要让 AI 做两次以上，就该封装成 Skill，而不是每次重新打一遍提示词。

Skill 本质上是一份 markdown 说明书，告诉模型这类任务该怎么做、用什么工具、输出什么格式。

**举例**：一个"生成周报"的 Skill

```markdown
---
name: weekly-report
description: 根据本周的 git commit 和已关闭 issue 生成周报
---

# 周报生成 Skill

## 步骤
1. 运行 `git log --since="7 days ago" --oneline`
2. 运行 `gh issue list --state closed --search "closed:>=7 days ago"`
3. 按照下面的模板输出，不要遗漏任何 feat/fix 类型的 commit
4. bugfix 和 feature 分开列，纯 refactor 不列入周报

## 输出模板
### 本周完成
- ...
### 已修复问题
- ...
```

**踩坑提醒**（来自实践经验）：
- 不要做"万金油 Skill"，一个 Skill 只做一件事，做窄做深
- 一定要写清楚输入验证和边界情况，否则模型会自己脑补
- 别重复造轮子，先看看已有 Skill 库里有没有类似的

---

## 第四层：Handover 文档 —— 会话之间的接力棒

这是最后一道保险，专门解决"当前这个任务做一半，对话要关了"的问题。

**触发时机**：当前任务还没做完，但对话已经很长、模型开始出现前后矛盾或者重复劳动的迹象时。

**做法**：让模型自己总结当前状态，写成一个文件，然后你开新对话，第一句话就指向这个文件。

```markdown
# Handover: 2026-07-24 用户认证重构

## 当前状态
- 已完成：注册接口改造，测试全绿
- 进行中：登录接口的 JWT 刷新逻辑，卡在 refresh token 的存储位置没定
- 未开始：登出接口的 token 黑名单机制

## 关键决策
- refresh token 决定不存 Redis，改存 Postgres 的 `refresh_tokens` 表，
  原因见 ADR-0007（Redis 重启会丢失导致强制登出所有用户）

## 遇到的坑
- `middleware/auth.ts` 里有一段历史遗留代码会拦截 OPTIONS 请求，改动前注意

## 下一步
1. 把 refresh token 表的 migration 写完（`migrations/`目录，跟着现有命名规则）
2. 补登录接口的刷新逻辑
3. 跑一遍集成测试 `npm run test:integration`
```

新会话开场白就是：

> 请读 `docs/sessions/2026-07-24-auth-refactor.md`，我们接着做。

这看起来"笨"，比让模型在一个巨长的对话里连续工作要稳得多——你相当于用一份文件替代了几十轮对话的隐性上下文，而且这份文件本身就是可以复查、可以给同事看的。

---

## 整体工作流长什么样

把四层串起来，一个典型的任务生命周期是这样的：

```
新会话开始
   │
   ▼
模型自动读 CLAUDE.md（项目常识）
   │
   ▼
如果任务涉及某个历史决策 → 按需读对应 ADR
   │
   ▼
如果任务是重复性操作 → 触发对应 Skill
   │
   ▼
Plan Mode：只读探索 + 出计划，你审批
   │
   ▼
执行、测试、提交
   │
   ▼
任务未完成，对话变长 → 生成 handover 文档，关闭对话
   │
   ▼
（回到"新会话开始"，读 handover 文档接着做）
```

任务完成后，如果这次改动涉及新的架构决策，别忘了回头补一条 ADR；如果发现某个操作又重复做了第三次，考虑封装成 Skill。这四层文档本身也需要维护，不是写完就一劳永逸。

---

## 常见误区

- **误区一**：把 CLAUDE.md 写成大百科全书。—— 越长模型每次要读的 token 越多，该精简就精简。
- **误区二**：什么决策都写 ADR。—— 只写"不记录会被重新犯错"的那些。
- **误区三**：用"记得住"来衡量 AI 靠不靠谱。—— 应该问的是"这个信息有没有落盘成文件"，而不是"上一轮我说过它应该记得"。
- **误区四**：handover 文档写完就不管了。—— 任务真正完成后，有价值的部分应该回流到 CLAUDE.md 或 ADR 里，handover 本身用完就可以归档甚至删除。

---

## 一句话总结

**对话是易失的，文件是持久的。** 把项目常识放 CLAUDE.md，把"为什么"放 ADR，把重复劳动打包成 Skill，把未完成任务的状态存成 handover 文档——这四样东西才是 AI 编程真正的"记忆"，聊天记录从来都不是。
